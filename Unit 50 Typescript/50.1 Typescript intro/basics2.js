var score = 4;
var color = 'green';
var food;
var arr = [];
// arr.toLowerCase()
var evens = [2, 4, 6, 7]; // TS knows its an array of numbers
var colors = ['red', 'orange']; // TS knows its an array of strings
// colors.push(true) // throws an error if pushing a different type on to typed array
var numBoolArray = [1, true, 0, false, 4, true, false];
var highScore = 1;
