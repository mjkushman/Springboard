//This is al copied from servers-test
describe("Test helper functions (with setup and tear-down)", function() {
    beforeEach(function () {
      // initialization logic
    });
  
    //test sumPaymentTotal function
    it('should sum up all payments of given type with sumPaymentTotal(type)', function () {
        
        sumPaymentTotal();


    });
   
    //test calculateTipPercent(billAmt, tipAmt) function
    it('table body should have a row appended with id of key', function () {
      // Prep

        //run the function
        calculateTipPercent(billAmt, tipAmt)
        
    });
  
  
    //test appendTd(tr, value) function

    appendTd(tr, value)

    afterEach(function() {
      serverNameInput.value ='';
      allPayments = {};
    });
  });
  